import { historyDates } from "./data/events";

export const NUM_DOTS = historyDates.length;
export const CIRCLE_WIDTH = 535;
export const RADIUS = CIRCLE_WIDTH / 2;

export const ANGLE_PER_DOT = 360 / NUM_DOTS;
export const TARGET_ANGLE = 300;
